apiVersion: v2
name: monkeypuzzle
description: |
  The Monkeypuzzle global piece registry with private developer accounts,
  dashboard and APIs. Bundles Postgres; PR monitoring and Temporal are optional.
type: application

# Chart version — bump on chart changes.
version: 0.2.0

# Default container image tag (overridable via image.tag). Tracks the server release.
appVersion: "0.1.0"

home: https://monkeypuzzle.dev
sources:
  - https://github.com/jewell-lgtm/monkeypuzzle
keywords:
  - monkeypuzzle
  - stacked-prs
  - mcp
  - developer-tools
maintainers:
  - name: Matt Jewell
    url: https://github.com/jewell-lgtm
