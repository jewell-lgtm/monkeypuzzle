---
title: add user-level config via GAP
status: done
description: "Use muesli/go-app-paths (GAP) for user-level config/cache/templates. Potential uses: user default preferences, reusable templates, auth cache."
---

# add user-level config via GAP

Use muesli/go-app-paths (GAP) for user-level config/cache/templates. Potential uses: user default preferences, reusable templates, auth cache.
