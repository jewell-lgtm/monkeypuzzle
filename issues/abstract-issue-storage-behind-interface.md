---
title: Abstract issue storage behind interface
status: todo
description: Current implementation uses file-based issue storage directly. Refactor to use an interface pattern to allow pluggable backends (GitHub Issues, Jira, Linear, etc.).
---

# Abstract issue storage behind interface

Current implementation uses file-based issue storage directly. Refactor to use an interface pattern to allow pluggable backends (GitHub Issues, Jira, Linear, etc.).
