---
const commands = [
  { cmd: 'mp init', desc: 'Initialize project' },
  { cmd: 'mp piece create', desc: 'Create new piece' },
  { cmd: 'mp piece update', desc: 'Sync with main' },
  { cmd: 'mp piece merge', desc: 'Merge to main' },
  { cmd: 'mp piece pr create', desc: 'Create GitHub PR' },
  { cmd: 'mp issue create', desc: 'Create issue' },
];
---

<section class="py-24 px-4 border-t border-bg-elevated">
  <div class="container mx-auto max-w-3xl">
    <h2 class="text-3xl font-bold text-center mb-4">
      Commands
    </h2>
    <p class="text-muted text-center mb-12">
      Everything you need, nothing you don't
    </p>
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
      {commands.map((item) => (
        <div class="squircle flex items-center gap-4 p-4">
          <code class="text-accent-pink font-mono text-sm whitespace-nowrap">
            {item.cmd}
          </code>
          <span class="text-muted text-sm">
            {item.desc}
          </span>
        </div>
      ))}
    </div>
    <p class="text-center mt-8 text-muted text-sm">
      All commands support <code class="text-accent-pink">--schema</code> for JSON discovery
    </p>
  </div>
</section>
