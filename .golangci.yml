version: "2"

linters:
  exclusions:
    paths:
      # Piece worktrees are separate checkouts of this repo; lint them on their
      # own branches, not as part of the main module.
      - \.monkeypuzzle/
